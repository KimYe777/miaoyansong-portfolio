# Industrial Editorial Portfolio · DESIGN.md

> 设计方向：工业编辑部
> 版本：2026-08-07（中文原页展示修订）
> 状态：已确认中文优先；详情页按原作品集页序完整展示

## 1. 设计意图

网站首页像一本经过精密编排的工业设计年鉴：画面温暖但不怀旧，结构理性但不冰冷。进入项目后，网站退到背景，让已经完成排版的作品集原页成为唯一主角。

关键词：`editorial`、`precise`、`tactile`、`quiet confidence`、`process-led`。

## 2. 参考来源与转译

- Refero / ElevenLabs：借鉴暖白画布、轻字重、极少颜色和编辑式层级。
- Refero / Augen Pro：借鉴大留白、细线边界、精准对齐和内容最大宽度。
- Refero / Harvest：借鉴温暖中性色与单一橙色强调。
- MotionSites / 3D Portfolio、Prosthetics Hero：借鉴首屏中“一个物体就是一个主角”的构图。
- MotionSites / Lumenvox Atelier：借鉴媒体驱动的首屏和分段文字揭示，不采用液态玻璃装饰。
- React Bits：仅使用内容进入、媒体显现和可选模型查看类组件。

这些参考只提供设计原则，不复制其品牌资产、文案或完整布局。

## 3. 颜色系统

```css
:root {
  --paper: #f3f0e8;
  --paper-bright: #fbfaf6;
  --ink: #171714;
  --ink-soft: #4f4b45;
  --muted: #787168;
  --hairline: rgba(23, 23, 20, 0.16);
  --accent: #f0542d;
  --accent-dark: #b93617;
  --selection: #ffd4c5;
}
```

规则：

- `--paper` 是全站主画布，避免纯白屏幕感。
- `--accent` 只用于项目编号、链接状态、关键标记与少量交互反馈。
- 大面积项目媒体保持素材原色，不套统一橙色滤镜。
- 不引入第二个高饱和 UI 强调色。
- 正文与背景对比度至少满足 WCAG AA。

### 项目章节配色

全站导航、首页文字和通用控件继续使用纸白 / 墨黑 / 橙色系统。进入项目媒体章节后，允许通过项目容器变量切换背景、文字和强调色，以保留原作品的设计语言：

- `ClearSense`：冰蓝 `#d9eef4`、森林绿 `#173e35`、白色 `#f8fbfa`。
- `NightCare`：黑色 `#090b0c`、冷白 `#f1f3f3`、提示青 `#39d7d2`。
- `租前眼`：奶油白 `#f4f0e6`、深绿 `#173f32`、薄荷绿 `#bcd9c5`。
- `出去晃晃`：奶油白 `#f7f2e8`、深蓝 `#202b68`、薰衣草紫 `#d9d0ec`、珊瑚红 `#ff655b`。

项目色仅服务于章节识别、媒体衬底和重点信息，不改变全站链接、焦点状态及基础可访问性规则。

## 4. 字体系统

- Display：`Instrument Serif`，备用 `Georgia, serif`。
- Sans / UI / Body：`Manrope`，备用 `Arial, sans-serif`。
- 中文标题：`Noto Serif SC`、`Songti SC`、`STSong`、衬线字体。
- 中文正文与导航：`Noto Sans SC`、`Microsoft YaHei`、无衬线字体。
- Metadata：沿用 Manrope，使用大写、较小字号与增加字距，不额外引入等宽字体。

建议字号：

| 角色 | 桌面 | 手机 | 行高 |
|---|---:|---:|---:|
| Hero | clamp(64px, 8vw, 132px) | 52px | 0.9–0.96 |
| Page title | 64px | 40px | 1.0 |
| Section title | 40px | 30px | 1.08 |
| Project title | 28px | 24px | 1.12 |
| Body large | 20px | 18px | 1.5 |
| Body | 16px | 16px | 1.6 |
| Metadata | 11px | 10px | 1.3 |

## 5. 布局与间距

- 内容最大宽度：1440px；正文阅读列最大宽度：720px。
- 桌面网格：12 列；平板：8 列；手机：4 列。
- 页面边距：桌面 40–64px，平板 28px，手机 18px。
- 基础间距单位：4px；主要节奏使用 8 / 16 / 24 / 40 / 64 / 96 / 144px。
- 首页项目之间使用 120–180px 垂直距离，让每个项目形成独立章节。
- 文字、图片和说明始终共享可见的对齐线。

## 6. 形状与边界

- 页面内容以直角与 1px hairline 为主。
- 项目媒体允许 2–8px 小圆角，仅用于避免图像边缘过硬。
- 按钮使用文本链接或紧凑矩形，不使用大面积胶囊按钮。
- 不使用常规卡片阴影；层级由纸张色差、留白和边界表达。

## 7. 首页构图

1. 顶部细线导航：姓名 / Work / About / Email。
2. 首屏：一句清晰定位、当前状态、一个代表性产品主视觉。
3. Selected Work：ClearSense、NightCare 与日迹使用大幅横向或非对称编辑布局，三个工业设计项目均提供真实内容入口。
4. Digital Work：租前眼与出去晃晃使用更紧凑的双列或交错布局。
5. About 摘要：照片或工作过程图 + 个人方法描述。
6. Footer：邮箱、简历下载、所在地和版权信息。

## 8. 项目详情页构图

- 顶部只保留中文返回入口、项目名称、页数和查看提示。
- 从原PDF第1页开始，按原页码连续展示到最后1页，不删减、不重排、不改写。
- 每一页使用完整页面图像，保持原宽高比，不裁切，不在页面上覆盖解释文字。
- 页面之间使用窄而稳定的间距，形成连续翻阅节奏。
- 末尾显示中文“下一个项目”入口。
- 手机端同样保持完整页面比例，允许内容随屏幕缩小，不拆解原页布局。

## 9. 图片与视频

- 首页 hero 素材建议宽度至少 2400px；详情页大图至少 1800px。
- 同一项目优先选择 1 张 hero、2–4 张过程图、2–4 张结果图。
- 每张图承担明确叙事角色，不做无意义画廊堆叠。
- 视频默认静音、可暂停，并提供静态 poster。
- 未来如有 `.glb/.gltf`，Model Viewer 作为可选增强，不能替代静态图片。

## 10. 动效规范

- 首屏：标题按行进入，产品主视觉随后轻微上移和显现；总时长不超过 1.2 秒。
- 滚动：章节与媒体使用 16–32px 位移 + opacity，避免大幅飞入。
- 项目悬停：图像轻微缩放至 1.015，标题与编号改变强调色。
- 页面切换：150–250ms 淡入，不做全屏遮罩动画。
- 所有动效使用自然减速，禁止无限循环装饰动画。
- `prefers-reduced-motion: reduce` 下取消位移与连续动画。

## 11. 禁止项

- 紫色科技渐变、彩色 Aurora、液态金属和网格粒子背景。
- 光标尾迹、持续磁吸、页面抖动和故障文字。
- 用大量圆角卡片把项目切成 SaaS Dashboard。
- 为了“显得丰富”虚构统计数字或客户评价。
- 让动画延迟项目内容出现，或阻止用户正常滚动。

## 12. 当前项目叙事顺序

1. `ClearSense`：最完整的实体产品、服务与 App 系统，作为首页首项和代表项目。
2. `NightCare`：校园夜间健康支持终端，以黑色全宽媒体形成节奏转折。
3. `日迹`：家庭照片分享终端，通过老人终端、家人手机与实体打印动作形成完整服务闭环。
4. `租前眼`：围绕首次租房判断与证据记录的决策辅助 App。
5. `出去晃晃`：围绕无目的散步、AI 路线与长期足迹的轻量体验 App。

首页只提供快速理解所需的中文摘要和封面媒体；完整作品集在独立详情页按PDF原顺序连续展示。此前“不连续展示PDF页面”的规则被本次用户明确要求覆盖。

## 13. ClearSense 独立互动入口

- ClearSense 的完整作品集、产品模型和 UI 原型是三个并列入口，不把互动内容追加在原PDF页面之后。
- 作品集详情页仍只按原页序连续展示，不插入、替代或重排任何一页。
- 产品模型使用独立页面；旋转、缩放之外，用户可直接拖住上方流线型产品整体离开底座，内部零件不散开。
- 模型外观依据项目渲染图重建珍珠白 PBR 材质、冷灰蓝凹槽、结构阴影和摄影棚光照。
- UI 原型使用独立页面和黑色真实手机模型框；原型直接填满屏幕，不增加灰色外层展示框。
- 模型和原型均只在各自路由加载，失败时提供静态图片或独立打开入口。
- 详细验收标准见 `docs/superpowers/specs/2026-08-07-clearsense-independent-experiences-redesign.md`。

## 14. UI 项目独立原型入口

- “租前眼”和“出去晃晃”各自提供“完整作品集”和“体验 UI 原型”两个并列入口，不虚构实体产品或产品模型。
- 原作品集继续在详情页按原顺序完整展示；交互原型位于独立路由，不插入作品集页面。
- 两个原型均沿用项目原文件与原交互，只复制到网站发布目录，不修改源项目。
- 独立体验页使用统一黑色手机模型框；原型铺满手机屏幕，不叠加额外灰框或第二层手机外壳。

## 15. 日迹独立联动体验

- 日迹的完整作品集与“模型和双端联动”是两个并列入口，不在原 PDF 页面之间插入交互内容。
- 体验页使用日迹自身的米白、橄榄绿和陶土色；模型为桌面端视觉主体，双端原型位于右侧切换区域。
- 家人手机发送照片后，老人终端出现一条新内容提醒；只有观看者在老人终端点击“打印照片”，模型才从右侧真实出纸位置推出同一张照片。
- 照片推出后保持伸出，并随模型共同旋转；“取走照片”同时重置模型和老人终端状态。
- 移动端使用“产品模型 / 老人终端 / 家人手机”三个标签，两个原型在设备框内整体等比例缩放，不要求用户缩放浏览器。
- 网页使用约 20 万三角面、6.8MB 的轻量 GLB；加载失败时退回日迹静态产品渲染图。
- 详细验收标准见 `docs/superpowers/specs/2026-08-08-riji-portfolio-and-linked-experience-design.md`。

### 15.1 日迹模型材质与屏幕修正

- 整机材质以日迹正式渲染图 A 为准：暖白细砂前壳、深灰侧后壳、浅灰哑光金属结构。
- 屏幕边界和显示比例以屏幕细节渲染图 C 为准；动态内容必须完整嵌入真实屏幕区域，不使用悬浮卡片。
- 轻量 GLB 只有一个整体网格，网页使用程序化 PBR 材质按局部结构重建外观，不继续使用发白噪点旧材质，也不采用单视角图片投影。
- 初始屏幕关闭，家人发送后才显示内容；模型屏幕、老人终端和打印照片始终使用同一份分享数据。
- 材质与屏幕必须在正面、左右 45°和侧面完成贴合验收，并保持 360°旋转、手机触摸和打印流程。
- 详细验收标准见 `docs/superpowers/specs/2026-08-08-riji-model-material-screen-correction-design.md`。
- 日迹轻量模型底部的碎面不通过全局重新简化解决；使用暖白独立底座几何遮盖破损下沿，并把浅灰金属限制在独立抽屉正面，避免底部整体误染。

### 15.2 日迹可选语音与抽屉交互

- 家人手机端保留录制声音的路径，同时允许在录音页选择“跳过录音，只发照片”。
- 无语音分享仍同步照片、标题、留言和日期；老人终端必须明确显示“没有声音”，不能播放预设语音。
- 模型底部灰色抽屉可独立点击开合，不与照片发送、终端接收或打印状态绑定。
- 抽屉开合提供模型直接点击与可聚焦按钮两种入口，并在减少动态效果模式下直接切换终点。
- 详细验收标准见 `docs/superpowers/specs/2026-08-08-riji-optional-voice-and-drawer-design.md`。
